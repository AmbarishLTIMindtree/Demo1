Explain Ratings and Resource

First explain how many record type we have before going to scenarios.

which time zone we are using to set time?

Set Message as : The reservation cannot be in past.

Please make correction in email alert(make it as 'Created') and Change in Mail body message.

Make Changes in page layout(add two columns).

Difference between ISBLANK() AND ISEMPTY().

Make changes in when status is inactive. Use isNew() and ISPICKVAL() [use case 2].

Make '<=' in use case 3.

select 'created' for use cases 4 and 5.

Search for the alternative approach for use case 6.

use case 9: Which one we have to choose[created or (created or updated)]?

